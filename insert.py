# import mysql.connector as mysql
# mysdb=mysql.connect(
# host='localhost',
# user='root',
# password='root123',
# database='workers')
# mycursor=mysdb.cursor()
# name=input('enter the name')
# age=int(input('enter the number'))
# mycursor.execute('insert into ajayvijayan(name,age)values(%s,%s)',(name,age))
# mysdb.commit()
# print('done')